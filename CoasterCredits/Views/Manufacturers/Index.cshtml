@model IEnumerable<CoasterCredits.Models.Manufacturer>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ManufacturerName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ManufacturerLocation)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ManufacturerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ManufacturerLocation)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ManufacturerID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ManufacturerID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ManufacturerID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
